---
name: api-endpoints
description: "EdgeShield Dashboard API documentation."
metadata:
  languages: "markdown"
  tags: "api,rest,endpoints"
  versions: "1.0.0"
  source: "maintainer"
---

# API Endpoints

## GET /api/v1/stats
Returns aggregated security statistics for the dashboard.

## GET /api/v1/events
Returns a stream of recent security events.

## POST /api/v1/nodes/deploy
Deploys a new edge node to a specified region.
